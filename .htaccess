# Some Apache configs don't map .mjs to a JS MIME type by default, which
# makes browsers reject it as an ES module (strict MIME checking). Needed
# for the pdfjs-dist worker used by the PDF Scopy search tool.
AddType text/javascript .mjs
